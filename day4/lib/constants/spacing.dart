/// Spacing and layout dimension constants.
class AppSpacing {
  AppSpacing._();

  /// Horizontal padding inside white container card
  static const double horizontalPadding = 14.0;

  /// Top padding inside white container card
  static const double topPadding = 14.0;

  /// Radius for top corners of the main surface card (42.0px)
  static const double cardTopRadius = 42.0;

  /// Gap between custom app bar and workout header
  static const double appBarToHeader = 12.0;
}
